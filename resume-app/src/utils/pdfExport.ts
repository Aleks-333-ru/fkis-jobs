/**
 * Экспорт в PDF = печать текущего DOM.
 * Никакого второго макета: в PDF уходит ровно то, что видно в предпросмотре,
 * причём текст остаётся текстом (его можно выделять и искать), а не картинкой.
 */
export function exportToPdf() {
  window.print()
}
